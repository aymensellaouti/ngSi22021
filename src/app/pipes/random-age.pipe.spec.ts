import { RandomAgePipe } from './random-age.pipe';

describe('RandomAgePipe', () => {
  it('create an instance', () => {
    const pipe = new RandomAgePipe();
    expect(pipe).toBeTruthy();
  });
});
